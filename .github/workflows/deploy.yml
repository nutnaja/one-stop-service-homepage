name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write # เพิ่มสิทธิ์ให้เขียนไฟล์ได้

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # ดึงข้อมูลทั้งหมด รวมถึง history

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm" # เพิ่ม cache เพื่อเร่งความเร็ว

      - name: Install dependencies
        run: npm install

      - name: List project files
        run: ls -la

      - name: Build and Generate static files
        run: |
          npm run build
          npm run generate
          echo "Checking output directory:"
          ls -la .output/public || ls -la dist

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
          folder: .output/public # หรือ dist ขึ้นอยู่กับการตรวจสอบในขั้นตอนก่อนหน้า
          clean: true # ล้างไฟล์เก่าก่อน deploy
